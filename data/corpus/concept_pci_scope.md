# Concept: PCI DSS Scope

Any system that stores, processes, or transmits raw card data falls in PCI DSS scope. Tokenization keeps the PAN out of merchant systems, shrinking scope. Logs must never contain a full PAN; masking is mandatory before storage.
