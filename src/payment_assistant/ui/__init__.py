"""Presentation layer: static web UI assets (HTML/CSS/JS). Contains no business logic.

The files under ``static/`` are served by the API process (see ``api/app.py``); there is
no Python here beyond this marker, and no build step.
"""
